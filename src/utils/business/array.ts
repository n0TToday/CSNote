// export function ato(arr: Array<JSON>) {
// const res = [];
// for(cosnt i in arr)
// }

// import type { Ref } from 'vue';

// export function ota(objs: Record<string, string>[], name1: string, name2: string) {
//   const res = [];
//   for (const obj in Object.values(objs)) {
//     res.push({ name1: obj[0], name2: obj[1] });
//   }
//   return res;
// }
